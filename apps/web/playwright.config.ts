import { defineConfig, devices } from "@playwright/test";

/**
 * Phase 0 has exactly one E2E test. It starts the app locally and asserts
 * the landing page renders. Full signup -> /app flows belong to later
 * phases where they exercise real product behavior; Phase 0's acceptance
 * criterion for E2E is deliberately narrow.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: Boolean(process.env.CI),
  retries: process.env.CI ? 1 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: {
    command: "pnpm dev",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});